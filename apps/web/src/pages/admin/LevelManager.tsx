export const LevelManager = () => (
  <div className="space-y-4">
    <h1 className="font-display text-3xl font-bold">Level Manager</h1>
    <p className="text-sm text-slate-700">
      This screen is ready for CRUD forms backed by the `/api/admin/levels` endpoints.
    </p>
  </div>
);
